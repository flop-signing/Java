package enums;

public enum Status {
    // Here Status is like a class and the value of enum is object of each status

    // Every value in Enum are like named constant.

    Running,Failed,Pending,Success
}
